import { Typography } from "@mutualzz/ui-native";
import { observer } from "mobx-react-lite";

const SpacesIndex = () => {
    return <Typography>How did you end up here</Typography>;
};

export default observer(SpacesIndex);
