import { ProfileWidgetEditorScreen } from "@components/Profile/widgets/editor/ProfileWidgetEditorScreen";
import { observer } from "mobx-react-lite";

const ProfileWidgetsRoute = () => {
  return <ProfileWidgetEditorScreen />;
};

export default observer(ProfileWidgetsRoute);
