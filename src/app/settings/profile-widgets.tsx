import { ProfileWidgetsEditorScreen } from "@components/UserSettings/ProfileWidgetsEditorScreen";
import { observer } from "mobx-react-lite";

export default observer(function ProfileWidgetsRoute() {
  return <ProfileWidgetsEditorScreen />;
});
