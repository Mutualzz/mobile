import { FeedSnapScreen } from "@components/Feed/FeedSnapScreen";
import { observer } from "mobx-react-lite";

const FeedSavedScreen = () => <FeedSnapScreen variant="saved" />;

export default observer(FeedSavedScreen);
